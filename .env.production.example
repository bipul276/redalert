# Production Environment Configuration
# Copy this file to .env and fill in the values for your production server

# Database Credentials (Change these to strong, secure passwords in production)
POSTGRES_USER=redalert_prod
POSTGRES_PASSWORD=your_secure_db_password_here
POSTGRES_DB=redalert_prod

# Security Keys
# Generate a secure key (e.g., using `openssl rand -hex 32`)
SECRET_KEY=your_secure_random_secret_key_here

# URLs (CRITICAL: These must be set to your actual domain names)
# The URL where your frontend is hosted (e.g., https://redalert.yourdomain.com)
FRONTEND_URL=https://redalert.yourdomain.com

# The public URL where your backend API is accessible from the user's browser
# Ensure no trailing slash
NEXT_PUBLIC_API_URL=https://api.redalert.yourdomain.com/api/v1

# Push Notification Keys (VAPID)
# Generate new keys for production using `npx web-push generate-vapid-keys` or the provided scripts
VAPID_PUBLIC_KEY=your_production_vapid_public_key_here
VAPID_PRIVATE_KEY=your_production_vapid_private_key_here
VAPID_MAILTO=mailto:admin@yourdomain.com
